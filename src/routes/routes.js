import Home from "pages/home";
import Places from "pages/places";

const routes = [
  { id: 0, path: "/", name: "home", Component: Home },
  { id: 0, path: "/place-to-stay", name: "places", Component: Places },
];

export default routes;
